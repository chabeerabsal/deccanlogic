#include<iostrem>
using namespace std;
int main()
{
	string s="geeksforgeeks";
	const int char=256;
	boolean visitied[char]={0};
	fill(visited,viside+char,false);
	int res=-1;
	for(int i=str.length()-1;i>=0;i--)
	{
		if(visited(str[i])
		   str=i;
		   else
		   visited(str[i])=true;
	}
	return res
	
	}
}
